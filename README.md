# Main
ShellOS is simulating real os work. It doesn't have access to your PC and can't do anything to your PC.

## Installation
Now ShellOS requires installed these packages:
```sh
make python >= 3.10
```
